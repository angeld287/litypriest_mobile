import React from 'react';
import { View, Text } from 'react-native';

const LocationContactsContainer = () => {
	return (
		<View>
			<Text>LocationContactsContainer</Text>
		</View>
	);
};

export default LocationContactsContainer;
